import { Card, Text } from "@webpack/common";

export function NoEmojiSounds() {
    return (
        <Card style={{ padding: "36px", display: "flex", justifyContent: "center" }}>
            <Text>No emoji sounds!</Text>
        </Card>
    );
}